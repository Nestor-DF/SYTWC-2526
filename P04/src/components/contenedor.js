import * as React from 'react'

const Contenedor = ({ children }) => {
    return (
        <div>
            <h1>Espacio Cultural</h1>
            {children}
        </div>
    )
}

export default Contenedor
